---

    - name: ping machine cible
      hosts: wordpress
      become: yes
      tasks:
        - name: ping_machine_cible
          ping:

    - name: Install nginx
      package:
        name: nginx
        state: present

    - name: Start nginx
      service:
        name: nginx
        state: started
      
