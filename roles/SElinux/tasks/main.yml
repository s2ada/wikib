- name: Disable SELinux
  selinux:
    state: disabled
  become: yes


vi /etc/selinux/config

SELINUX=disabled
---
- hosts: localhost
  tasks:
  - lineinfile:
      path: /etc/selinux/config
      regexp: '^SELINUX='
      line: 'SELINUX=disabled'
